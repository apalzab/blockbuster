$('#movies').append('<%= j show_movies(@movies).html_safe %>');
<% if @movies.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @movies %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>