<%= form_for Purchase.new, remote: true do |f| %>
  <%= f.hidden_field :movie_purchase_option_id, value: movie_purchase_option.id %>
  <%= f.submit "#{movie_purchase_option.quality.upcase} - #{number_to_euro(movie_purchase_option.price)}", class: 'col-md-8 btn btn-warning' %>
<% end %>
